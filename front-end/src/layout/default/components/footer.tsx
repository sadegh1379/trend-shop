import type { FC } from "react";
import { FooterContainer } from "../css/footer";

const Footer: FC = () => {
  return (
    <FooterContainer>
      <p className="title">Trend Shop</p>
    </FooterContainer>
  );
};

export default Footer;
